# 安装依赖
```bash
pnpm install
pnpm run installRuntime
# if got err: No module named 'distutils'
pip install setuptools
#BM25所用库
pip install jieba nltk pandas json5 matplotlib
```
# 开始开发
```bash
pnpm run dev
```